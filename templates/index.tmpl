{{template "base" .}}
{{define "title"}}Index{{end}}
{{define "content"}}
	<div class="container">
		<h1>SDE Search</h1>
		<p>Use the search bar in the top right corner to look for type information</p>
	</div>
{{end}}